import React from 'react';
import { Link } from 'react-router';

class LaunchComponent extends React.Component {
  constructor(props){
    super(props);
  }

  render(){
    return (
      <div>

      </div>
    )
  }
}

export default connect(null, null)(LaunchComponent);